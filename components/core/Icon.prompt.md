Lucide line icon tinted with the current text color; use for every UI glyph.
```jsx
<Icon name="shopping-bag" size={20} />
```
Names come from lucide.dev. Default 20px; 16px inline with small text, 24px in headers.